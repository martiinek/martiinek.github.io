Place your hero image here as `martin.jpg`.

This project references `/martin.jpg` from the public root. You can replace it with any filename, but if you do, update the path in `components/Hero.tsx` where `backgroundImage` is defined.
